<h3>Course <%= @course.title %> Lesson <%= @lesson.title %> </h3>

<%= link_to "Edit", edit_author_course_lesson_path(@course, @lesson) %>

<%= link_to 'Destroy', author_course_lesson_path(@course, @lesson), method: :delete, data: { confirm: 'Are you sure?' } %>