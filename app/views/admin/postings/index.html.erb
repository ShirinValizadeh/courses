<p id="notice"><%= notice %></p>

<h1>Postings</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @postings.each do |posting| %>
      <tr>
        <td><%= posting.title %></td>
        <td><%= posting.description %></td>
        <td><%= link_to 'Show', admin_posting_path(posting) %></td>
        <td><%= link_to 'Edit', edit_admin_posting_path(posting) %></td>
        <td><%= link_to 'Destroy', admin_posting_path(posting), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Posting', new_admin_posting_path %>
